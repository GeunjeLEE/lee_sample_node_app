version: 2.1

orbs:
  aws-ecr: circleci/aws-ecr@0.0.2

jobs:
  checkout:
    executor: circleci/node:12.16.1
    steps:
      - checkout

workflows:
  build:
    jobs:
      - checkout
      - aws-ecr/build_and_push_image:
          requires:
            - checkout
          account-url: "${AWS_ACCOUNT_ID}.dkr.ecr.${AWS_DEFAULT_REGION}.amazonaws.com"
          repo: "sample-ecr"
          region: ${AWS_DEFAULT_REGION}
          tag: "latest"
